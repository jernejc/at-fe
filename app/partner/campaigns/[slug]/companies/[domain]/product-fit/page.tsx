'use client';

import { BarChart3 } from 'lucide-react';

/** Product fit analysis page — placeholder until feature is built out. */
export default function CompanyProductFitPage() {
  return (
    <div className="flex flex-col items-center justify-center gap-3 py-24 text-center">
      <BarChart3 className="size-10 text-muted-foreground/50" />
      <p className="text-sm font-medium text-foreground">Product Fit</p>
      <p className="text-sm text-muted-foreground">Coming soon</p>
    </div>
  );
}
